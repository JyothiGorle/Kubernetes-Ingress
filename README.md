# Kubernetes-Ingress
Ingress &amp; probes


Before cloning or after cloning the project install this Ingress controller inorder to generate the DNS Name(to hoist the application using pathbased/Hostbased)

kubectl create -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.2.1/deploy/static/provider/cloud/deploy.yaml
